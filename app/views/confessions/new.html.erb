<h1>New Confession</h1>
<%= form_for @confession do |f| %>
	<%= f.label :title %><br>
	<%= f.text_field :title %><br><br>
	<%= f.label :text %><br>
	<%= f.text_area :text %><br>
	<%= f.collection_check_boxes :topics, Topic.all, :id, :name %>
	<%= f.submit 'Post Confession' %>
<% end %>
