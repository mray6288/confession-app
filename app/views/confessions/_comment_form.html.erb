<%= form_for @confession do |f|%>
	<%=f.fields_for :comments, @confession.comments.build do |comment| %>
		<%= comment.text_field :text%>
		<%= comment.hidden_field :user_id, value: current_user.id %>

		
	<% end %>
	
	
	<%= f.submit value:'Create Comment'%>
<% end %>